import AdminLayout from '@/components/layout/admin.layout';
import React from 'react';

type Props = {};

const Adminage = (props: Props) => {
  return <AdminLayout>Admin Dasboard</AdminLayout>;
};

export default Adminage;
